#include <stdio.h>
#include <string.h>

int main() {
 
    char str[] = "Nobody_Noticed_Him";


    char pattern[] = "Him";

    for (i=0 , i<n-m , j++)
        {
        	j=0
        	while j<m and P[j]==t[i+j]
        	       j=j+1
        	       if (j==m)
        	        return (i)
		}
